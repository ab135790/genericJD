/*
  通过mutation间接更新state的多个方法的对象
 */
import {
    // RECEIVE_NEWHOUSE,
} from './mutation-types';

const actions = {
    // 存储新房详情数据
    // getCurFooterNum({ commit }, index) {
    //     // 提交一个mutation
    //     if (index) {
    //         commit(getCurFooterNum, {index});
    //     }
    // },
};
export default actions;
